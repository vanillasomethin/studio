{
  "name": "alive-footfall-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone MQTT subscriber that turns RuView (WiFi CSI) + ESPresense (BLE) signals into footfall counts and screen-presence correlation for ALIVE stores.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "dotenv": "^16.4.5",
    "mqtt": "^5.10.1"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  },
  "engines": {
    "node": ">=20"
  }
}
